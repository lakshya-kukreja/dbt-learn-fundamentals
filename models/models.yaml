version: 2

models:
  - name: dim_customers  # This should match your CURRENT .sql filename
    description: "The final customer dimension table used for analytics."
    columns:
      - name: customer_id
        description: "Primary key. Joined dynamically using ref() to support all developer environments."